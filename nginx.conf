worker_processes auto;

events { worker_connections 1024; }

http {
    upstream backend {
        server web:80;
    }

    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://backend;
        }
    }
}
